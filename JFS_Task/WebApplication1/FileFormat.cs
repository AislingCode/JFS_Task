namespace JFS_Task
{
    /// <summary>
    /// It is easier to manage when each enum is defined inside its own file.
    /// </summary>
    public enum FileFormat
    {
        None = 0,
        XML = 1,
        CSV = 2
    }
}
